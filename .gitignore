# Local .terraform directories
**/.terraform/*

# .tfstate files
*.tfstate
*.tfstate.*

# terraform locks
.terraform.lock.hcl

# Crash log files
crash.log

# Generated files
pkg/tfstate/variables.tf
pkg/tfstate/config.auto.tfvars
pkg/tfstate/tfstate-locals.tf
terraform-backend.tf

# Ignore any .tfvars files
#
*.tfvars
!/config.auto.tfvars

# Ignore override files as they are usually used to override resources locally and so
# are not checked in
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# tflint
.tflint.hcl

#EKS context file
kubeconfig_*

# Include override files you do wish to add to version control using negated pattern
#
# !dc_override.tf

# Include tfplan files to ignore the plan output of command: terraform plan -out=tfplan
# example: *tfplan*

# IDEs
**/.idea/*
.DS_Store
